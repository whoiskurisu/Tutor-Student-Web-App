document.addEventListener('DOMContentLoaded', function () {
    // Automatically scroll the visual out after page loads
    setTimeout(() => {
        document.querySelector('.scroll-visual').style.display = 'none';
    }, 2000); // 2 seconds for the scroll effect
});

